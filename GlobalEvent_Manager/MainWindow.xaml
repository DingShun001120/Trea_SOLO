<Window x:Class="GlobalEventManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="事件管理器演示" Height="420" Width="680">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="用户名:" VerticalAlignment="Center"/>
            <TextBox Width="160" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Content="启用过滤（只接受以 A 开头）" IsChecked="{Binding FilterEnabled}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10,0,10" >
            <Button Content="发布登录事件（UI线程）" Command="{Binding PublishLoginUiCommand}"/>
            <Button Content="发布重活事件（后台线程）" Command="{Binding PublishHeavyBackgroundCommand}"/>
            <Button Content="演示多订阅优先级" Command="{Binding PublishPriorityDemoCommand}"/>
            <Button Content="清空日志" Command="{Binding ClearLogsCommand}"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="状态："/>
            <TextBlock Grid.Row="0" Margin="48,0,0,8" Text="{Binding StatusText}"/>

            <ListBox Grid.Row="1" ItemsSource="{Binding Logs}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
    
</Window>